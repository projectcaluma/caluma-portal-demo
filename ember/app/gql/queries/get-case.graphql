query getCaseQuery($filter: [CaseFilterSetType]) {
  allCases(filter: $filter) {
    edges {
      node {
        id
        status
        createdAt
        modifiedAt
        document {
          id
          form {
            name
            description
          }
        }
      }
    }
  }
}
