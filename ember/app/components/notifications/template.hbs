<div>
  {{#each this.notifications.all as |notification|}}
    <div class="uk-alert uk-alert-{{this.notification.type}}">
      <a href="" class="uk-alert-close" uk-close {{on "click" (fn this.remove notification)}}></a>
      {{notification.message}}
    </div>
  {{/each}}
</div>
